$MACRO PathFuncs;
DEF_STR(S, A, B, C, D, E);
S := '  C:\\TEST DIR\\NAME.EXT  ';
A := REMOVE_SPACE(S);
B := GET_EXTENSION(A);
C := GET_PATH(A);
D := TRUNCATE_EXTENSION(A);
E := TRUNCATE_PATH(A);
TVCALL MessageBox(A, B, C, D, E);
END_MACRO;
