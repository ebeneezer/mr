$MACRO clone_line_above;
	DEF_CHAR(CurrentCh);
	UP;
	CurrentCh := CUR_CHAR;
	DOWN;
	TEXT(CurrentCh);
END_MACRO;

$MACRO smart_backspace;
	IF BLOCK_STAT <> 0 THEN
	    DELETE_BLOCK;
	ELSE
	    BACK_SPACE;
	END;
END_MACRO;
