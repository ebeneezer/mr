$MACRO TEST;

IF BLOCK_STAT = 0 THEN
   TVCALL MessageBox('NULL');
END;
IF BLOCK_STAT = 1 THEN TVCALL MessageBox('1'); END;
IF BLOCK_STAT = 2 THEN TVCALL MessageBox('2'); END;
IF BLOCK_STAT = 3 THEN TVCALL MessageBox('3'); END;

END_MACRO;