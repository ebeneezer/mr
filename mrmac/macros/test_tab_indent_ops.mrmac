$MACRO TestTabIndentOps;
DEF_STR(S);
DEF_INT(I);
TAB_EXPAND := TRUE;
INSERT_MODE := TRUE;
TAB_RIGHT;
TAB_LEFT;
INDENT;
UNDENT;
I := INDENT_LEVEL;
SET_INDENT_LEVEL;
S := GET_LINE;
EXPAND_TABS(S);
TABS_TO_SPACES(S);
END_MACRO;
