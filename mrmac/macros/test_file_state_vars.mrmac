$MACRO TestFileStateVars;
DEF_INT(FS, EIM, BID, TF, FC);
DEF_STR(TFN, FN);
FS := FIRST_SAVE;
EIM := EOF_IN_MEM;
BID := BUFFER_ID;
TF := TMP_FILE;
TFN := TMP_FILE_NAME;
FN := FILE_NAME;
FC := FILE_CHANGED;
FILE_CHANGED := FALSE;
FILE_NAME := 'state-test.txt';
END_MACRO;
